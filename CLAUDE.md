# リズムてんこもり — Claude Code 用メモ

甥っ子（小さい子ども）が親のスマホで遊ぶための、ブラウザで動くリズムゲーム集。
『リズム天国』シリーズの人気ゲームの**遊び方（コンセプト）だけ**を参考にしたオリジナル作品。

## 最重要ルール

- **著作権**：本家のキャラクター・絵・曲・効果音・名前（ゲーム名や決まり文句）は使わない、似せない。遊び方のアイデアだけを借りる。見た目・音はすべてオリジナル。
- **対象**：未就学〜小学校低学年くらいの子ども。文字はひらがな中心。UI はスマホ縦持ち・片手〜両手の親指操作が前提。
- **ボタンは1つ**（左右2ボタンは「意味がわからない」と不評だったので廃止済み）。
- **依頼者の好み**：「本家っぽさ」「リズムの取りやすさ」を重視。唐突な演出は嫌う（必ず予告・助走を入れる）。採点はシビアでよい。

## 構成

- `index.html` 1ファイルで完結（HTML/CSS/JS すべてインライン、ビルド不要）。
- 外部読み込みは Google Fonts（Mochiy Pop One / M PLUS Rounded 1c）のみ。
- 絵はすべて Canvas 2D でコード描画、音はすべて Web Audio API で合成（音声ファイルなし）。
- 保存は `localStorage`：
  - `tenkomori-settings` … 設定（判定のきびしさ・タイミング補正・けぬきの「せーの」とガイド、ゲームごとの BPM／難易度／長さ）
  - `tenkomori-highscore` … `{ gameId: { easy, normal, hard } }` 難易度ごとのハイスコア

### コードの地図（`<script>` 内）

1. **共通エンジン**
   - 音：`tone()` `noise()` `voice(t, 母音, f0, dur)`（フォルマント合成の こえ。わくぐりの「パピプペポ」。母音ごとの音量は `VOWEL_NORM` で そろえてある） `SND` `bgm()`（ループ伴奏） `songBgm(ev, t0, beat, nBars, style)`（Aメロ→Bメロ→サビの1曲。style = `"karate"` 短調 / `"kaeru"` 長調）
   - 時間：すべて `AudioContext.currentTime` 基準。`vnow()`＝画面表示用（出力遅延を引く）、`inTime()`＝入力判定用（出力遅延＋タイミング補正を引く）。
   - スケジューラ：`chart.ev`（{t, fn}）を 25ms ごとに先読み 0.2 秒で鳴らす `pump()`。
   - 判定：`press()` / `release()`（長押し）/ `hit()` / `miss()`。判定窓は `JUDGE`。どのノーツにも当たらない押下は「からぶり」（`strays`、1回 `STRAY_PENALTY` 点減点）。
   - 採点 `finish()`：バッチリ＝100 − ずれ量に応じて最大12点減、OK 55、おしい 20、ミス 0。全部バッチリ＆からぶり0のときだけ 100 点（それ以外は最大 99）。
   - 画面：メニュー → ゲームのタイトル（難易度3択＋ハイスコア）→ プレイ → 結果（ハイスコア更新時は「ハイスコア たっせい！おめでとう！」＋紙ふぶき＋ファンファーレ）。
2. **ゲームモジュール**（それぞれ `build(t0)` `phase(now)` `draw(now)` `onHit` `onMiss` `onFree` `floatXY` を持つオブジェクト。`GAMES` に登録）
   - `Keneki`（けぬき名人）… 本家「リズム脱毛」風。お手本の小節で毛がリズムよく生える →「じゅんび…せー・の！」の間（設定で 0/2/4 拍）→ 同じリズムで抜く。巻き毛＝長押し。
     - ピンセットのガイド：頭の円弧を延長した道を**一定の速さ**で進む（左下から回り込んで登場 → 毛の時刻にちょうどその毛の上 → 右下へ去る）。巻き毛の間だけその毛の上で止まって引っぱり、抜けたら元のペースに戻る。画面に出るのは常に1本。
   - `Karate`（カラテねこ）… 本家「カラテ家」風。1曲通し（ラウンドなし）。植木ばち（1拍で到達）・スイカ（2拍）・野球ボール（半拍、むずかしいのみ）。
     - 野球ボールは**ボール専用の小節**でだけ出す。小節の前半2拍が予告（「ピッ・ピッ」＋左端でボールがちらっと見える＋「！」）。ボールの次の小節は冒頭を空ける。
   - `Kaeru`（ケロっと わくぐり）… 本家「輪くぐり」風。1曲通し。
     - パピプペポは**常に等間隔**。変化は「1文字の速さ `sp`」と「次の輪までの休み `rest`」（`PLAN`）でつける。曲が進むほど難しいプランが出る。
     - 輪は**止まらず一定速度**で右→左：パ＝画面右端、プ＝奥の仲間、ペ＝手前の仲間、ポ＝キミ（プレイヤー）の前を通過。仲間は輪が来た瞬間にジャンプの頂点が来るように跳ぶ。
     - 輪は手前半分／奥半分に分けて描き、カエルが輪の中を通って見えるようにしている。
3. **絵の共通パーツ**（`bigText` のすぐ下）：`INK`（線の色） `ol()`（ぬり＋太いくろフチ） `shade()`（形の中だけに かげを1段） `burst()`（ドカン！のギザギザ） `sparkle()` `blush()`（ほっぺ＋ななめ線） `focusLines()`（集中線） `rays()`（放射の背景） `motionLines()`（いきおい線）。
   - 絵のテイストは「太い線のシンプル漫画風」：くろい太フチ、ベタぬり＋かげ1段、白いツヤ1つ。グラデーションは使わない。
4. **難易度の上がり方**：`pickLevel()` で前半は簡単なパターン → 後半ほど複雑（たまに1段下がってひと息）。各ゲームの最初は必ずいちばん基本のリズム。

## 動作確認

- ローカル：`index.html` をブラウザで開くだけ（音はボタンを押したあとに鳴る＝ブラウザの自動再生制限のため）。
- スマホ確認：`npx serve .` などで LAN に出してスマホから開く。iPhone はマナーモードだと音が出ない。
- 自動確認：Playwright でゲームを開始 → スペースキーで押下 → スクリーンショット、という流れで確かめてきた。連打で 0 点になること（からぶり減点）も確認済み。

## 次にやること

1. ~~絵のクオリティアップ~~（2026-09-21 済：A案＝Canvas のまま描き込み、太い線の漫画風で3ゲームとも）
   - 残り：メニュー・説明画面の SVG アイコンは まだ前のタッチのまま
2. **GitHub に載せて GitHub Pages で公開**（リポジトリ作成 → push → Settings > Pages で main ブランチ / root を公開）
3. （あれば）2人で交代して遊ぶ協力モード、PWA 化（ホーム画面に追加でアプリっぽく）
